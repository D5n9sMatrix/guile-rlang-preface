#!/usr/bin/r

#######################################################################################
## apples heart forth poisson linear equation books pdf guides rcode editor manual
## help document books and pdf to rcode manual series apples pdf evolution linears
## examples:
##########################################################################################
apples <- function (x, y)
{
     x <- matrix(data = x, nrow = x, ncol = y, byrow = FALSE, dimnames = NULL)
     y <- matrix(data = x, nrow = x, ncol = y, byrow = FALSE, dimnames = NULL)

     log(c(m = x, d = x, k = y, p = y))
}
##########################################################################################

make.link